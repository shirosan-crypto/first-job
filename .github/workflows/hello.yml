name: Hello World

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World!"

      - uses: actions/checkout@v3

      - run: ls -la
